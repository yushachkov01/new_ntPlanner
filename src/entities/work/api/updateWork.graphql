mutation UpdateWork($id: Int!, $set: works_set_input!) {
  update_works_by_pk(pk_columns: { id: $id }, _set: $set) {
    id
    date
    project
    site
    description
    time_range
    status
    ppr_hours
    work_hours
    overtime_hours
  }
}
