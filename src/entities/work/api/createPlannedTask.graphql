mutation createPlannedTask($object: planned_tasks_insert_input!) {
  insert_planned_tasks_one(object: $object) {
    id
    name
    description
    rm_task_id
    yaml_url
    time_work_id
    author_id
  }
}
